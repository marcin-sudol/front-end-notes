const Link = window.ReactRouterDOM.Link;

const Error = () => {
  return (
    <div>
      <button>
        <Link to="/">Return to home</Link>
      </button>
    </div>
  );
};

export default Error;
